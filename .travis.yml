language: cpp
compiler:
  - gcc
  - clang

git:
  depth: 3
  submodules: true

branches:
  only:
  - master
  - develop

matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-8.0
            - george-dision55-precise-backports
          packages:
            - clang-8.0
            - cmake-data
            - cmake
      env:
        - MATRIX_EVAL="CC=clang-8.0 && CXX=clang++-8.0"

before_install:
    - eval "${MATRIX_EVAL}"
